apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-react-app
spec:
  ingressClassName: example-react-app
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: example-react-app
            port:
              number: 80